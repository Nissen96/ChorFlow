# Explicit data flow policy

# Global policy
u.name   -> rp.user
rp.user  -> ip.id, s.class
ip.id    -> rp.report
s.class  -> s.data
s.data   -> u.info, s.data
u.my_pwd -> ip.pwd

# Local policy/policies
SendData:
p.data -> q.info, p.data

